Script that update metadata in EXIF to match corresponding Voigtlander mapped to Leica lenses

To Run:

chmod +x voigtlenser.sh

./voigtlenser.sh ~/Pictures/someDir

It will update the lens data for all files in that directory, and enable lens corrections. 
You need to reload metadata in Lightroom if they are already part of your catalogue.
